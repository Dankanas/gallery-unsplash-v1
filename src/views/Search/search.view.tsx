import React, { FC } from 'react';

import ImageSearchContainer from '../../containers/ImageSearch.container';

const Search : FC = () => {
    return <ImageSearchContainer/>
}

export default Search;